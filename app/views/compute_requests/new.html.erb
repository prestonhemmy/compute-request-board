<% content_for :title, "New compute request" %>

<div class="max-w-2xl mx-auto">
  <h1 class="font-bold text-4xl">New request</h1>

  <%= render "form", compute_request: @compute_request %>

  <%= link_to "Back to compute requests", compute_requests_path,
              class: "block w-full text-center rounded-md mt-2 px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 font-medium" %>
</div>
